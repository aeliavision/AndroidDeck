<resources>
    <!--
        STD-AND03: Best practices for 2026 include using the SplashScreen API
        and ensuring the theme supports edge-to-edge (transparent status/nav bars).
    -->
    <style name="Theme.AndroidDeck" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/surface</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_notification</item>
        <item name="postSplashScreenTheme">@style/Theme.AndroidDeck</item>
    </style>
</resources>

<UserControl x:Class="VcfEditor.Views.GroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource Brush.Background}">

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource HeroPanel}">
            <StackPanel>
                <TextBlock Text="Groups" Style="{StaticResource PageTitleText}" />
                <TextBlock Text="Organize contacts into groups and smart collections." Style="{StaticResource PageSubtitleText}" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource ModernCard}" Margin="0,0,0,0">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="&#xE902;" FontFamily="Segoe MDL2 Assets" FontSize="{StaticResource IconSize.HeroDisplay}" Foreground="{DynamicResource Brush.Primary}" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="Groups workspace is ready for the next feature pass." FontSize="{StaticResource FontSize.BodyLarge}" FontWeight="SemiBold" Foreground="{DynamicResource Brush.TextPrimary}" TextAlignment="Center" />
                <TextBlock Text="The layout now matches the modern app style." FontSize="{StaticResource FontSize.Body}" Foreground="{DynamicResource Brush.TextSecondary}" TextAlignment="Center" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
